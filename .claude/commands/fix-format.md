# /fix-format — Fix Formatting

Auto-fix formatting issues across the project.

## Instructions

1. Run `npx prettier --write .` to fix formatting.
2. Report what was changed:
   - Number of files reformatted by Prettier
   - Any files that could not be formatted
3. Run `npm run check` to verify TypeScript types are still valid.
