---
phase: 04-evaluation-infrastructure
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/evaluation/__init__.py
  - backend/evaluation/metrics.py
  - backend/evaluation/patterns.py
  - backend/evaluation/logger.py
  - backend/evaluation/test_metrics.py
  - backend/evaluation/test_patterns.py
  - backend/evaluation/test_logger.py
  - backend/requirements.txt
autonomous: true
requirements: [EVAL-01, EVAL-03]

must_haves:
  truths:
    - 'jiwer library is installed and importable'
    - 'Test stubs exist for metrics, patterns, and logger modules'
    - 'All test stubs fail (RED state) because implementations are empty'
  artifacts:
    - path: 'backend/evaluation/__init__.py'
      provides: 'Package init'
    - path: 'backend/evaluation/metrics.py'
      provides: 'WER/CER calculation stubs'
      contains: 'def calculate_wer'
    - path: 'backend/evaluation/patterns.py'
      provides: 'Error pattern extraction stubs'
      contains: 'def extract_error_patterns'
    - path: 'backend/evaluation/logger.py'
      provides: 'JSONL logging stubs'
      contains: 'def log_evaluation'
    - path: 'backend/evaluation/test_metrics.py'
      provides: 'Tests for WER/CER calculation'
      contains: 'def test_'
    - path: 'backend/evaluation/test_patterns.py'
      provides: 'Tests for error pattern extraction'
      contains: 'def test_'
    - path: 'backend/evaluation/test_logger.py'
      provides: 'Tests for JSONL logging'
      contains: 'def test_'
  key_links:
    - from: 'backend/evaluation/test_metrics.py'
      to: 'backend/evaluation/metrics.py'
      via: 'import'
      pattern: 'from evaluation.metrics import'
    - from: 'backend/evaluation/test_patterns.py'
      to: 'backend/evaluation/patterns.py'
      via: 'import'
      pattern: 'from evaluation.patterns import'
    - from: 'backend/evaluation/test_logger.py'
      to: 'backend/evaluation/logger.py'
      via: 'import'
      pattern: 'from evaluation.logger import'
---

<objective>
Wave 0: Create test scaffolding and module stubs for the evaluation infrastructure.

Purpose: Establish the RED state for TDD — tests define expected behavior before implementation.
Output: Test files with failing tests, empty module stubs, jiwer dependency installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evaluation-infrastructure/04-RESEARCH.md
@.planning/phases/04-evaluation-infrastructure/04-VALIDATION.md
@backend/main.py
@backend/requirements.txt
@backend/dialects.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install jiwer and create evaluation package with module stubs</name>
  <files>
    backend/requirements.txt,
    backend/evaluation/__init__.py,
    backend/evaluation/metrics.py,
    backend/evaluation/patterns.py,
    backend/evaluation/logger.py
  </files>
  <read_first>
    backend/requirements.txt,
    backend/tests/test_offset_filter.py,
    .planning/phases/04-evaluation-infrastructure/04-RESEARCH.md
  </read_first>
  <action>
1. Add `jiwer==3.1.1` and `python-json-logger==3.2.1` to `backend/requirements.txt` (append after existing entries).

2. Create `backend/evaluation/__init__.py` with content:

```python
"""Evaluation infrastructure for dialect transcription quality measurement."""
```

3. Create `backend/evaluation/metrics.py` with stub functions:

```python
"""WER/CER calculation using jiwer library."""
from jiwer import wer, cer, process_words
import jiwer

# Standard text normalization for WER/CER comparison
NORMALIZATION = jiwer.Compose([
    jiwer.ToLowerCase(),
    jiwer.RemoveMultipleSpaces(),
    jiwer.Strip(),
    jiwer.RemovePunctuation()
])

def calculate_wer(reference: str, hypothesis: str) -> float:
    """Calculate Word Error Rate between reference and hypothesis.

    Args:
        reference: Ground truth text (what user corrected it to)
        hypothesis: Raw transcription output (what ASR produced)

    Returns:
        WER as float (0.0 = perfect, 1.0 = 100% errors)
    """
    raise NotImplementedError("Plan 04-01 implements this")

def calculate_cer(reference: str, hypothesis: str) -> float:
    """Calculate Character Error Rate between reference and hypothesis.

    Returns:
        CER as float (0.0 = perfect)
    """
    raise NotImplementedError("Plan 04-01 implements this")

def calculate_metrics(reference: str, hypothesis: str) -> dict:
    """Calculate all metrics (WER, CER, detailed alignment).

    Returns:
        Dict with keys: wer, cer, substitutions, deletions, insertions, total_words
    """
    raise NotImplementedError("Plan 04-01 implements this")
```

4. Create `backend/evaluation/patterns.py` with stub functions:

```python
"""Error pattern extraction from WER alignment."""
from jiwer import process_words
import jiwer

def extract_error_patterns(reference: str, hypothesis: str) -> dict:
    """Extract substitution/deletion/insertion patterns from alignment.

    Args:
        reference: Ground truth text
        hypothesis: Raw transcription output

    Returns:
        Dict with keys: substitutions (int), deletions (int), insertions (int),
        total_words (int), error_rate (float), details (list of error dicts)
    """
    raise NotImplementedError("Plan 04-01 implements this")

def categorize_errors(reference: str, hypothesis: str) -> list[dict]:
    """Return list of individual errors with type and context.

    Returns:
        List of dicts: [{"type": "substitution"|"deletion"|"insertion",
                         "expected": str, "actual": str}]
    """
    raise NotImplementedError("Plan 04-01 implements this")
```

5. Create `backend/evaluation/logger.py` with stub functions:

```python
"""JSONL evaluation logger — append-only, no raw text (privacy)."""
import json
from pathlib import Path
from datetime import datetime, timezone

DEFAULT_LOG_PATH = Path(__file__).parent.parent / "eval_logs"

def log_evaluation(
    session_id: str,
    dialect_region: str,
    wer: float,
    cer: float,
    substitutions: int,
    deletions: int,
    insertions: int,
    total_words: int,
    low_confidence_count: int = 0,
    feedback: str | None = None,
    log_dir: Path = DEFAULT_LOG_PATH,
) -> Path:
    """Append evaluation metrics to JSONL file.

    PRIVACY: Never log raw transcription text. Only aggregate metrics.

    Returns:
        Path to the log file written to.
    """
    raise NotImplementedError("Plan 04-01 implements this")

def read_evaluations(log_dir: Path = DEFAULT_LOG_PATH, limit: int = 100) -> list[dict]:
    """Read recent evaluation entries from JSONL files.

    Returns:
        List of evaluation dicts, newest first, up to limit.
    """
    raise NotImplementedError("Plan 04-01 implements this")
```

6. Run `cd /Users/xavierbuise/Dev/babbel/backend && pip install jiwer==3.1.1 python-json-logger==3.2.1` to install the dependency.
   </action>
   <verify>
   <automated>cd /Users/xavierbuise/Dev/babbel/backend && python -c "import jiwer; print(jiwer.**version**)" && python -c "from evaluation.metrics import calculate_wer; print('metrics stub OK')" && python -c "from evaluation.patterns import extract_error_patterns; print('patterns stub OK')" && python -c "from evaluation.logger import log_evaluation; print('logger stub OK')"</automated>
   </verify>
   <acceptance_criteria> - backend/requirements.txt contains line "jiwer==3.1.1" - backend/requirements.txt contains line "python-json-logger==3.2.1" - backend/evaluation/**init**.py exists - backend/evaluation/metrics.py contains "def calculate_wer(" - backend/evaluation/metrics.py contains "def calculate_cer(" - backend/evaluation/metrics.py contains "def calculate_metrics(" - backend/evaluation/patterns.py contains "def extract_error_patterns(" - backend/evaluation/patterns.py contains "def categorize_errors(" - backend/evaluation/logger.py contains "def log_evaluation(" - backend/evaluation/logger.py contains "def read_evaluations(" - `python -c "import jiwer"` exits 0
   </acceptance_criteria>
   <done>All 4 evaluation module files exist with stub functions, jiwer installed successfully</done>
   </task>

<task type="auto" tdd="true">
  <name>Task 2: Create test scaffolding for metrics, patterns, and logger</name>
  <files>
    backend/evaluation/test_metrics.py,
    backend/evaluation/test_patterns.py,
    backend/evaluation/test_logger.py
  </files>
  <read_first>
    backend/tests/test_offset_filter.py,
    backend/evaluation/metrics.py,
    backend/evaluation/patterns.py,
    backend/evaluation/logger.py,
    .planning/phases/04-evaluation-infrastructure/04-RESEARCH.md
  </read_first>
  <behavior>
    - test_wer_identical_texts: WER("hello world", "hello world") == 0.0
    - test_wer_completely_wrong: WER("hello world", "foo bar") == 1.0
    - test_wer_partial_match: WER("de kat zit op de mat", "de hond zit op de mat") has 0.0 < wer < 1.0
    - test_cer_identical: CER("abc", "abc") == 0.0
    - test_cer_partial: CER("hallo", "halo") returns float > 0.0
    - test_metrics_returns_all_fields: calculate_metrics returns dict with keys wer, cer, substitutions, deletions, insertions, total_words
    - test_normalization: WER ignores case and punctuation ("Hello!" vs "hello" == 0.0)
    - test_extract_patterns_counts: extract_error_patterns returns correct substitution/deletion/insertion counts
    - test_categorize_errors_types: categorize_errors returns list of dicts with type field
    - test_log_creates_jsonl: log_evaluation creates a .jsonl file in log_dir
    - test_log_appends: multiple log_evaluation calls append to same daily file
    - test_log_no_raw_text: JSONL output does NOT contain any field with raw text content
    - test_log_has_required_fields: JSONL entries contain timestamp, session_id, dialect_region, wer, cer, substitutions, deletions, insertions, total_words
    - test_read_evaluations: read_evaluations returns list of dicts from JSONL files
  </behavior>
  <action>
Create 3 test files following the existing pattern in `backend/tests/test_offset_filter.py` (sys.path insert for imports).

1. Create `backend/evaluation/test_metrics.py`:

```python
"""Tests for WER/CER calculation — RED state (stubs raise NotImplementedError)."""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))

import pytest
from evaluation.metrics import calculate_wer, calculate_cer, calculate_metrics


class TestWER:
    def test_identical_texts_zero_wer(self):
        assert calculate_wer("hello world", "hello world") == 0.0

    def test_completely_wrong_full_wer(self):
        assert calculate_wer("hello world", "foo bar") == 1.0

    def test_partial_match(self):
        wer_score = calculate_wer("de kat zit op de mat", "de hond zit op de mat")
        assert 0.0 < wer_score < 1.0

    def test_normalization_ignores_case_and_punctuation(self):
        assert calculate_wer("Hello, world!", "hello world") == 0.0

    def test_empty_reference_raises(self):
        with pytest.raises((ValueError, ZeroDivisionError)):
            calculate_wer("", "some text")


class TestCER:
    def test_identical_texts_zero_cer(self):
        assert calculate_cer("abc", "abc") == 0.0

    def test_partial_difference(self):
        cer_score = calculate_cer("hallo", "halo")
        assert cer_score > 0.0

    def test_normalization_ignores_case(self):
        assert calculate_cer("Hello", "hello") == 0.0


class TestMetrics:
    def test_returns_all_required_fields(self):
        result = calculate_metrics("de kat zit op de mat", "de hond zit op de mat")
        assert "wer" in result
        assert "cer" in result
        assert "substitutions" in result
        assert "deletions" in result
        assert "insertions" in result
        assert "total_words" in result

    def test_substitutions_counted(self):
        result = calculate_metrics("de kat zit", "de hond zit")
        assert result["substitutions"] >= 1

    def test_total_words_correct(self):
        result = calculate_metrics("een twee drie vier", "een twee drie vier")
        assert result["total_words"] == 4
```

2. Create `backend/evaluation/test_patterns.py`:

```python
"""Tests for error pattern extraction — RED state."""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))

import pytest
from evaluation.patterns import extract_error_patterns, categorize_errors


class TestExtractPatterns:
    def test_returns_counts(self):
        result = extract_error_patterns("de kat zit", "de hond zit")
        assert "substitutions" in result
        assert "deletions" in result
        assert "insertions" in result
        assert "total_words" in result

    def test_substitution_detected(self):
        result = extract_error_patterns("de kat zit", "de hond zit")
        assert result["substitutions"] >= 1

    def test_deletion_detected(self):
        result = extract_error_patterns("de kat zit op de mat", "de kat zit de mat")
        assert result["deletions"] >= 1

    def test_insertion_detected(self):
        result = extract_error_patterns("de kat zit", "de grote kat zit")
        assert result["insertions"] >= 1

    def test_perfect_match_zero_errors(self):
        result = extract_error_patterns("hallo wereld", "hallo wereld")
        assert result["substitutions"] == 0
        assert result["deletions"] == 0
        assert result["insertions"] == 0


class TestCategorizeErrors:
    def test_returns_list_of_dicts(self):
        errors = categorize_errors("de kat zit", "de hond zit")
        assert isinstance(errors, list)
        assert len(errors) >= 1

    def test_error_has_type(self):
        errors = categorize_errors("de kat zit", "de hond zit")
        for err in errors:
            assert "type" in err
            assert err["type"] in ("substitution", "deletion", "insertion")

    def test_substitution_has_expected_and_actual(self):
        errors = categorize_errors("de kat zit", "de hond zit")
        subs = [e for e in errors if e["type"] == "substitution"]
        assert len(subs) >= 1
        assert "expected" in subs[0]
        assert "actual" in subs[0]
```

3. Create `backend/evaluation/test_logger.py`:

```python
"""Tests for JSONL evaluation logger — RED state."""
import sys
import json
import tempfile
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))

import pytest
from evaluation.logger import log_evaluation, read_evaluations


class TestLogEvaluation:
    def test_creates_jsonl_file(self, tmp_path):
        result_path = log_evaluation(
            session_id="test-123",
            dialect_region="mestreechs",
            wer=0.35, cer=0.22,
            substitutions=5, deletions=2, insertions=1,
            total_words=42,
            low_confidence_count=8,
            feedback="thumbs_up",
            log_dir=tmp_path,
        )
        assert result_path.exists()
        assert result_path.suffix == ".jsonl"

    def test_appends_to_same_file(self, tmp_path):
        log_evaluation(session_id="a", dialect_region="limburgs",
                       wer=0.1, cer=0.05, substitutions=1, deletions=0,
                       insertions=0, total_words=10, log_dir=tmp_path)
        log_evaluation(session_id="b", dialect_region="limburgs",
                       wer=0.2, cer=0.1, substitutions=2, deletions=1,
                       insertions=0, total_words=20, log_dir=tmp_path)

        jsonl_files = list(tmp_path.glob("*.jsonl"))
        assert len(jsonl_files) == 1
        lines = jsonl_files[0].read_text().strip().split("\n")
        assert len(lines) == 2

    def test_has_required_fields(self, tmp_path):
        log_evaluation(session_id="test-456", dialect_region="zittesj",
                       wer=0.3, cer=0.15, substitutions=3, deletions=1,
                       insertions=1, total_words=30, log_dir=tmp_path)

        jsonl_file = list(tmp_path.glob("*.jsonl"))[0]
        entry = json.loads(jsonl_file.read_text().strip())

        required = ["timestamp", "session_id", "dialect_region", "wer", "cer",
                     "substitutions", "deletions", "insertions", "total_words"]
        for field in required:
            assert field in entry, f"Missing field: {field}"

    def test_no_raw_text_in_output(self, tmp_path):
        log_evaluation(session_id="privacy-test", dialect_region="limburgs",
                       wer=0.5, cer=0.3, substitutions=5, deletions=2,
                       insertions=1, total_words=50, log_dir=tmp_path)

        jsonl_file = list(tmp_path.glob("*.jsonl"))[0]
        content = jsonl_file.read_text()
        entry = json.loads(content.strip())

        # No field should contain text content (reference/hypothesis)
        text_fields = ["reference", "hypothesis", "text", "raw_text", "transcription"]
        for field in text_fields:
            assert field not in entry, f"Privacy violation: field '{field}' found in log"

    def test_optional_feedback_field(self, tmp_path):
        log_evaluation(session_id="fb-test", dialect_region="limburgs",
                       wer=0.2, cer=0.1, substitutions=1, deletions=0,
                       insertions=0, total_words=10, feedback="thumbs_down",
                       log_dir=tmp_path)

        jsonl_file = list(tmp_path.glob("*.jsonl"))[0]
        entry = json.loads(jsonl_file.read_text().strip())
        assert entry["feedback"] == "thumbs_down"


class TestReadEvaluations:
    def test_reads_entries(self, tmp_path):
        log_evaluation(session_id="r-1", dialect_region="limburgs",
                       wer=0.1, cer=0.05, substitutions=1, deletions=0,
                       insertions=0, total_words=10, log_dir=tmp_path)
        log_evaluation(session_id="r-2", dialect_region="mestreechs",
                       wer=0.2, cer=0.1, substitutions=2, deletions=1,
                       insertions=0, total_words=20, log_dir=tmp_path)

        entries = read_evaluations(log_dir=tmp_path)
        assert len(entries) == 2
        # Newest first
        assert entries[0]["session_id"] == "r-2"

    def test_respects_limit(self, tmp_path):
        for i in range(5):
            log_evaluation(session_id=f"lim-{i}", dialect_region="limburgs",
                           wer=0.1, cer=0.05, substitutions=1, deletions=0,
                           insertions=0, total_words=10, log_dir=tmp_path)

        entries = read_evaluations(log_dir=tmp_path, limit=3)
        assert len(entries) == 3
```

All tests must currently FAIL (NotImplementedError from stubs). Verify with:

```bash
cd /Users/xavierbuise/Dev/babbel/backend && python -m pytest evaluation/ -x 2>&1 | tail -5
```

Expected: all tests FAIL with NotImplementedError.
</action>
<verify>
<automated>cd /Users/xavierbuise/Dev/babbel/backend && python -m pytest evaluation/ --tb=line 2>&1 | grep -E "FAILED|ERROR|passed" | head -5</automated>
</verify>
<acceptance_criteria> - backend/evaluation/test_metrics.py contains "class TestWER" - backend/evaluation/test_metrics.py contains "class TestCER" - backend/evaluation/test_metrics.py contains "class TestMetrics" - backend/evaluation/test_patterns.py contains "class TestExtractPatterns" - backend/evaluation/test_patterns.py contains "class TestCategorizeErrors" - backend/evaluation/test_logger.py contains "class TestLogEvaluation" - backend/evaluation/test_logger.py contains "class TestReadEvaluations" - `cd backend && python -m pytest evaluation/ --tb=line` shows 0 passed (all FAILED or ERROR) - No test file contains "import main" (tests import from evaluation package)
</acceptance_criteria>
<done>All test files exist with comprehensive test cases, all tests fail because stubs raise NotImplementedError (RED state confirmed)</done>
</task>

</tasks>

<verification>
- `cd /Users/xavierbuise/Dev/babbel/backend && python -c "import jiwer"` exits 0
- `cd /Users/xavierbuise/Dev/babbel/backend && python -m pytest evaluation/ --tb=line` shows all tests FAIL
- `ls backend/evaluation/` shows __init__.py, metrics.py, patterns.py, logger.py, test_metrics.py, test_patterns.py, test_logger.py
</verification>

<success_criteria>

- jiwer 3.1.1 installed and importable
- 7 files in backend/evaluation/ (init + 3 modules + 3 test files)
- All tests are in RED state (fail with NotImplementedError)
- Test files import from evaluation package, not from main.py
  </success_criteria>

<output>
After completion, create `.planning/phases/04-evaluation-infrastructure/04-00-SUMMARY.md`
</output>
